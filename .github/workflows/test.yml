name: SSH Test

on: push

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: Run a command remotely
        run: |
          echo "${{ secrets.PRIVATE_SSH_KEY }}" | tr -d '\r' > key.pem
          chmod 400 key.pem
          ssh -i key.pem eladmin7302021@35.202.167.42
          pwd
          ls