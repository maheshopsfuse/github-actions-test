name: Shell commands

on: [push]

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: echo string
        run: eccho "Hello Mahesh"
      - name: multiline script
        run: |
          node -v
          npm -v